[server]
url = http://your.vps.ip:47291
api_key = CHANGE_ME

[file]
name = Budget.xlsx
# Unique lock id for this file. Give each file its own id so several files
# can share the same server/api_key without fighting over one lock. If left
# blank, the filename above is used as the id.
id = budget

[build]
exe_name = LockLauncher
